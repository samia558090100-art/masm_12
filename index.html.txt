<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>⚡ MASUM - Ultimate Guild Manager</title>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Rajdhani:wght@300;400;500;600;700&family=Exo+2:wght@100;200;300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #ff4444;
            --primary-dark: #cc0000;
            --secondary: #1a1a2e;
            --accent: #ffaa00;
            --background: #0f0f23;
            --card-bg: rgba(26, 26, 46, 0.9);
            --text: #ffffff;
            --text-muted: #b0b0b0;
            --success: #00ff88;
            --warning: #ffaa00;
            --error: #ff4444;
            --glass: rgba(255, 255, 255, 0.1);
            --neon-glow: 0 0 10px rgba(255, 68, 68, 0.7), 0 0 20px rgba(255, 68, 68, 0.5), 0 0 30px rgba(255, 68, 68, 0.3);
        }

        [data-theme="light"] {
            --primary: #007bff;
            --primary-dark: #0056b3;
            --secondary: #f8f9fa;
            --accent: #17a2b8;
            --background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            --card-bg: rgba(255, 255, 255, 0.95);
            --text: #2d3748;
            --text-muted: #4a5568;
            --success: #28a745;
            --warning: #ffc107;
            --error: #dc3545;
            --glass: rgba(255, 255, 255, 0.2);
            --neon-glow: 0 0 20px rgba(0, 123, 255, 0.5);
        }

        [data-theme="red"] {
            --primary: #dc3545;
            --primary-dark: #c82333;
            --secondary: #2d0000;
            --accent: #ff6b6b;
            --background: #1a0000;
            --card-bg: rgba(45, 0, 0, 0.9);
            --text: #ffffff;
            --text-muted: #ff9999;
            --success: #00ff88;
            --warning: #ffaa00;
            --error: #ff4444;
            --glass: rgba(255, 0, 0, 0.1);
            --neon-glow: 0 0 10px rgba(220, 53, 69, 0.7), 0 0 20px rgba(220, 53, 69, 0.5);
        }

        [data-theme="blue"] {
            --primary: #007bff;
            --primary-dark: #0056b3;
            --secondary: #001f3f;
            --accent: #7fdbff;
            --background: #001529;
            --card-bg: rgba(0, 31, 63, 0.9);
            --text: #ffffff;
            --text-muted: #7fdbff;
            --success: #2ecc40;
            --warning: #ffdc00;
            --error: #ff4136;
            --glass: rgba(0, 123, 255, 0.1);
            --neon-glow: 0 0 10px rgba(0, 123, 255, 0.7), 0 0 20px rgba(0, 123, 255, 0.5);
        }

        [data-theme="green"] {
            --primary: #28a745;
            --primary-dark: #1e7e34;
            --secondary: #004d00;
            --accent: #00ff88;
            --background: #001a00;
            --card-bg: rgba(0, 77, 0, 0.9);
            --text: #ffffff;
            --text-muted: #99ff99;
            --success: #00ff88;
            --warning: #ffaa00;
            --error: #ff4444;
            --glass: rgba(40, 167, 69, 0.1);
            --neon-glow: 0 0 10px rgba(40, 167, 69, 0.7), 0 0 20px rgba(40, 167, 69, 0.5);
        }

        [data-theme="purple"] {
            --primary: #6f42c1;
            --primary-dark: #5a2d91;
            --secondary: #2d1b69;
            --accent: #b19cd9;
            --background: #1a103d;
            --card-bg: rgba(45, 27, 105, 0.9);
            --text: #ffffff;
            --text-muted: #b19cd9;
            --success: #00ff88;
            --warning: #ffaa00;
            --error: #ff4444;
            --glass: rgba(111, 66, 193, 0.1);
            --neon-glow: 0 0 10px rgba(111, 66, 193, 0.7), 0 0 20px rgba(111, 66, 193, 0.5);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Rajdhani', 'Exo 2', sans-serif;
        }

        body {
            background: var(--background);
            color: var(--text);
            min-height: 100vh;
            padding: 20px;
            transition: all 0.3s ease;
            position: relative;
            overflow-x: hidden;
        }

        .video-background {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
            z-index: -2;
            opacity: 0.15;
            filter: brightness(0.3) contrast(1.2);
        }

        .overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.85);
            z-index: -1;
        }

        #particles-js {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            pointer-events: none;
        }

        .theme-selector {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 1000;
            display: flex;
            gap: 10px;
            background: var(--glass);
            backdrop-filter: blur(10px);
            padding: 10px;
            border-radius: 20px;
            border: 1px solid var(--glass);
        }

        .theme-btn {
            width: 30px;
            height: 30px;
            border-radius: 50%;
            border: 2px solid var(--text);
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: var(--neon-glow);
        }

        .theme-btn:hover {
            transform: scale(1.2);
            box-shadow: 0 0 15px currentColor;
        }

        .theme-btn[data-theme="dark"] { background: linear-gradient(135deg, #0a0a0a, #2d2d2d); }
        .theme-btn[data-theme="light"] { background: linear-gradient(135deg, #ffffff, #f0f0f0); }
        .theme-btn[data-theme="red"] { background: linear-gradient(135deg, #dc3545, #ff6b6b); }
        .theme-btn[data-theme="blue"] { background: linear-gradient(135deg, #007bff, #7fdbff); }
        .theme-btn[data-theme="green"] { background: linear-gradient(135deg, #28a745, #00ff88); }
        .theme-btn[data-theme="purple"] { background: linear-gradient(135deg, #6f42c1, #b19cd9); }

        .container {
            max-width: 1400px;
            margin: 0 auto;
            position: relative;
            z-index: 1;
        }

        .header {
            text-align: center;
            padding: 40px 30px;
            background: var(--glass);
            backdrop-filter: blur(20px);
            border-radius: 25px;
            margin-bottom: 30px;
            border: 1px solid var(--glass);
            box-shadow: var(--neon-glow);
            position: relative;
            overflow: hidden;
            border: 2px solid transparent;
            background-clip: padding-box;
        }

        .header::before {
            content: '';
            position: absolute;
            top: -2px;
            left: -2px;
            right: -2px;
            bottom: -2px;
            background: linear-gradient(45deg, var(--primary), var(--accent), var(--primary));
            border-radius: 25px;
            z-index: -1;
            animation: borderGlow 3s ease-in-out infinite alternate;
        }

        @keyframes borderGlow {
            0% { opacity: 0.7; }
            100% { opacity: 1; }
        }

        .brand {
            font-size: 4rem;
            font-weight: 900;
            background: linear-gradient(45deg, var(--primary), var(--accent));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 15px;
            text-shadow: 0 0 30px rgba(255, 68, 68, 0.5);
            font-family: 'Orbitron', sans-serif;
            letter-spacing: 3px;
            text-transform: uppercase;
        }

        .tagline {
            color: var(--text-muted);
            font-size: 1.3rem;
            margin-bottom: 20px;
            font-weight: 300;
            letter-spacing: 1.5px;
            font-family: 'Exo 2', sans-serif;
        }

        .main-tabs {
            display: flex;
            gap: 15px;
            margin-bottom: 30px;
            background: var(--glass);
            padding: 15px;
            border-radius: 20px;
            backdrop-filter: blur(10px);
        }

        .main-tab-btn {
            flex: 1;
            padding: 18px 25px;
            background: transparent;
            border: 2px solid transparent;
            border-radius: 15px;
            color: var(--text-muted);
            font-size: 1.2rem;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s ease;
            text-align: center;
            font-family: 'Orbitron', sans-serif;
            letter-spacing: 1px;
        }

        .main-tab-btn.active {
            background: var(--primary);
            color: white;
            border-color: var(--primary);
            transform: translateY(-3px);
            box-shadow: var(--neon-glow);
        }

        .main-tab-content {
            display: none;
            animation: fadeIn 0.3s ease;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .main-tab-content.active {
            display: block;
        }

        .card {
            background: var(--card-bg);
            border-radius: 25px;
            padding: 35px;
            backdrop-filter: blur(15px);
            border: 1px solid var(--glass);
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
            transition: all 0.4s ease;
            position: relative;
            overflow: hidden;
            margin-bottom: 30px;
        }

        .card:hover {
            transform: translateY(-8px);
            box-shadow: var(--neon-glow), 0 20px 60px rgba(0, 0, 0, 0.4);
        }

        .card-header {
            display: flex;
            align-items: center;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 3px solid var(--glass);
        }

        .card-icon {
            width: 70px;
            height: 70px;
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            border-radius: 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 20px;
            font-size: 2rem;
            box-shadow: var(--neon-glow);
        }

        .card-title {
            font-size: 2.2rem;
            font-weight: 700;
            color: var(--text);
            font-family: 'Orbitron', sans-serif;
            letter-spacing: 1px;
        }

        .form-group {
            margin-bottom: 25px;
        }

        .form-label {
            display: block;
            margin-bottom: 12px;
            color: var(--text-muted);
            font-weight: 600;
            font-size: 1.1rem;
        }

        .form-input, .form-select {
            width: 100%;
            padding: 18px 20px;
            background: var(--glass);
            border: 2px solid var(--glass);
            border-radius: 15px;
            color: var(--text);
            font-size: 1.1rem;
            transition: all 0.3s ease;
        }

        .form-input:focus, .form-select:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 4px rgba(255, 68, 68, 0.3);
            transform: scale(1.02);
        }

        .btn {
            padding: 18px 35px;
            border: none;
            border-radius: 15px;
            font-size: 1.2rem;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
            width: 100%;
            text-transform: uppercase;
            font-family: 'Orbitron', sans-serif;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            color: white;
        }

        .loading {
            display: none;
            text-align: center;
            padding: 50px;
            color: var(--text-muted);
        }

        .spinner {
            width: 60px;
            height: 60px;
            border: 4px solid var(--glass);
            border-left: 4px solid var(--primary);
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin: 0 auto 25px;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .result {
            margin-top: 25px;
            padding: 25px;
            border-radius: 20px;
            background: var(--glass);
            border: 2px solid var(--glass);
            display: none;
        }

        .result-content {
            font-family: monospace;
            white-space: pre-wrap;
            word-break: break-all;
        }

        .guild-info-display {
            max-height: 700px;
            overflow-y: auto;
        }

        .info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 25px;
        }

        .info-card {
            background: rgba(255, 255, 255, 0.05);
            padding: 20px;
            border-radius: 15px;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .info-label {
            color: var(--text-muted);
            font-size: 0.9rem;
            margin-bottom: 8px;
            text-transform: uppercase;
        }

        .info-value {
            font-size: 1.3rem;
            font-weight: 700;
            color: var(--accent);
        }

        @media (max-width: 768px) {
            .brand { font-size: 2.5rem; }
            .main-tabs { flex-direction: column; }
            .header { padding: 25px 20px; }
        }
    </style>
</head>
<body>
    <video class="video-background" autoplay muted loop playsinline>
        <source src="https://assets.mixkit.co/videos/preview/mixkit-abstract-geometric-particles-2-36939-large.mp4" type="video/mp4">
    </video>
    <div class="overlay"></div>
    <div id="particles-js"></div>

    <div class="theme-selector">
        <div class="theme-btn" data-theme="dark" title="Dark Theme"></div>
        <div class="theme-btn" data-theme="light" title="Light Theme"></div>
        <div class="theme-btn" data-theme="red" title="Red Theme"></div>
        <div class="theme-btn" data-theme="blue" title="Blue Theme"></div>
        <div class="theme-btn" data-theme="green" title="Green Theme"></div>
        <div class="theme-btn" data-theme="purple" title="Purple Theme"></div>
    </div>

    <div class="container">
        <div class="header">
            <div class="brand">⚡ MASUM GUILD MANAGER</div>
            <div class="tagline">Ultimate Free Fire Guild Information System</div>
        </div>

        <div class="main-tabs">
            <button class="main-tab-btn active" data-tab="info">🏆 GUILD INFORMATION</button>
        </div>

        <div class="main-tab-content active" id="infoTab">
            <div class="card">
                <div class="card-header">
                    <div class="card-icon">🔍</div>
                    <div class="card-title">SEARCH GUILD</div>
                </div>
                <form id="guildInfoForm">
                    <div class="form-group">
                        <label class="form-label" for="guildId">Guild ID</label>
                        <input type="text" class="form-input" id="guildId" placeholder="Enter Guild ID (e.g., 1234567)" required>
                    </div>
                    <div class="form-group">
                        <label class="form-label" for="region">Region</label>
                        <select class="form-select" id="region" required>
                            <option value="">Select Region</option>
                            <option value="BD">BD (Bangladesh)</option>
                            <option value="IND">IND (India)</option>
                            <option value="BR">BR (Brazil)</option>
                            <option value="US">US (United States)</option>
                            <option value="ME">ME (Middle East)</option>
                            <option value="PK">PK (Pakistan)</option>
                        </select>
                    </div>
                    <button type="submit" class="btn btn-primary"><span>🔍 SEARCH GUILD</span></button>
                </form>

                <div class="loading" id="infoLoading">
                    <div class="spinner"></div>
                    <div>Fetching guild information...</div>
                </div>

                <div class="result" id="infoResult">
                    <div class="result-content"></div>
                </div>
            </div>

            <div class="card" id="guildDisplayCard" style="display: none;">
                <div class="card-header">
                    <div class="card-icon">📊</div>
                    <div class="card-title">GUILD DETAILS</div>
                </div>
                <div class="guild-info-display" id="guildInfoDisplay"></div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/particles.js/2.0.0/particles.min.js"></script>
    <script>
        // API Configuration - আপনার দেওয়া লিঙ্কটি এখানে সেট করা হয়েছে
        const API_BASE_URL = 'https://guild-info-danger.vercel.app';

        // Particles JS Initialization
        particlesJS('particles-js', {
            particles: {
                number: { value: 80, density: { enable: true, value_area: 800 } },
                color: { value: "#ff4444" },
                shape: { type: "circle" },
                opacity: { value: 0.5, random: true },
                size: { value: 3, random: true },
                line_linked: { enable: true, distance: 150, color: "#ff4444", opacity: 0.4, width: 1 },
                move: { enable: true, speed: 3, direction: "none", random: true, out_mode: "out" }
            },
            interactivity: {
                detect_on: "canvas",
                events: { onhover: { enable: true, mode: "repulse" }, onclick: { enable: true, mode: "push" } }
            }
        });

        // Theme Switcher Logic
        document.querySelectorAll('.theme-btn').forEach(btn => {
            btn.addEventListener('click', () => {
                const theme = btn.getAttribute('data-theme');
                document.documentElement.setAttribute('data-theme', theme);
                localStorage.setItem('selected-theme', theme);
            });
        });

        // Guild Info Search Logic
        document.getElementById('guildInfoForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            const guildId = document.getElementById('guildId').value;
            const region = document.getElementById('region').value;
            const loading = document.getElementById('infoLoading');
            const result = document.getElementById('infoResult');
            const displayCard = document.getElementById('guildDisplayCard');

            loading.style.display = 'block';
            result.style.display = 'none';
            displayCard.style.display = 'none';

            try {
                // API Fetch - আপনার ফরম্যাট অনুযায়ী
                const response = await fetch(`${API_BASE_URL}/guild?guild_id=${guildId}&region=${region}`);
                const data = await response.json();

                if (response.ok) {
                    displayGuildData(data);
                } else {
                    result.style.display = 'block';
                    result.querySelector('.result-content').innerText = data.error || 'Failed to fetch data';
                }
            } catch (error) {
                result.style.display = 'block';
                result.querySelector('.result-content').innerText = 'Error: ' + error.message;
            } finally {
                loading.style.display = 'none';
            }
        });

        function displayGuildData(data) {
            const display = document.getElementById('guildInfoDisplay');
            const displayCard = document.getElementById('guildDisplayCard');
            
            // JSON ডেটা প্রদর্শনের সহজ উপায়
            display.innerHTML = `
                <div class="info-grid">
                    <div class="info-card">
                        <div class="info-label">Guild Name</div>
                        <div class="info-value">${data.guild_name || 'N/A'}</div>
                    </div>
                    <div class="info-card">
                        <div class="info-label">Guild Level</div>
                        <div class="info-value">${data.guild_level || 'N/A'}</div>
                    </div>
                    <div class="info-card">
                        <div class="info-label">Members</div>
                        <div class="info-value">${data.member_count || 'N/A'}</div>
                    </div>
                </div>
                <div class="info-card" style="margin-top: 20px;">
                    <div class="info-label">Full Raw Data</div>
                    <pre style="color: var(--text); overflow-x: auto;">${JSON.stringify(data, null, 2)}</pre>
                </div>
            `;
            displayCard.style.display = 'block';
        }
    </script>
</body>
</html>